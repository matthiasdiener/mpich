#
# rcsid = $Header: /nfs/globus1/src/master/nexus_examples/Makefile.in,v 1.6 1997/01/16 18:41:45 tuecke Exp $
#

all:
	for subdir in @SUBDIRS@; do \
	    echo "making all in $$subdir"; \
	    (cd $$subdir && $(MAKE) all) \
	done

install:

mostlyclean:
	for subdir in @SUBDIRS@; do \
	    echo "making mostlyclean in $$subdir"; \
	    (cd $$subdir && $(MAKE) mostlyclean) \
	done

clean:
	for subdir in @SUBDIRS@; do \
	    echo "making clean in $$subdir"; \
	    (cd $$subdir && $(MAKE) clean) \
	done

#
# config.cache is normally in ../, but put it here just to be safe
#
XTRA_FILES=config.cache \
	   config.log \
	   config.status \
	   Makefile
distclean:
	rm -f $(XTRA_FILES)
	for subdir in @SUBDIRS@; do \
	    echo "making distclean in $$subdir"; \
	    (cd $$subdir && $(MAKE) distclean) \
	done
